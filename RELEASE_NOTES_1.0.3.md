# WebVideo+ 1.0.3 发行说明

本次更新修复两处可能导致导出任务超时的引擎状态恢复问题。

- 当工程或 Terre 模板中的 WebGAL 运行时缺少导出所需的完整探针时，导出器会回退到内置 WebGAL 4.6.4 运行时，避免在规划阶段一直等待不存在的剧本解析器。探针完整的工程运行时仍会优先使用。
- 多进程导出恢复前文时，改用 WebGAL 解析出的有效语句位置。多行语句末尾的缩进续行不会再让导出器等待一个引擎实际不会到达的物理行号。
- 若仍发生启动或前文恢复超时，日志会分别记录引擎就绪状态、当前场景与语句位置，并列出本地 HTTP 4xx 资源线索，便于继续定位。

本机已验证无探针运行时的回退、完整探针运行时的保留，以及含有效多行语句的双进程导出。最初报错用户的运行环境仍需复测确认。

- 产品版本：`1.0.3`
- 安装器 Win32 版本：`1.0.3.0`
- 内部版本：`0.8.2`
- 导出内核：`0.6.43`
- 适配基线：WebGAL Terre `4.6.4`

正式安装器文件名为 `WebVideo+-Setup-1.0.3.exe`。此前版本的变更见 [CHANGELOG.md](CHANGELOG.md)。
