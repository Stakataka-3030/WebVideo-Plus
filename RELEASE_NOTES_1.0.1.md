# WebVideo+ 1.0.1 发行说明

本次更新重点改善 Live2D 模型在多进程导出时的切段连续性，并修复多行对话中的个别文字跳动。

## 修复与改进

- Cubism 3/4 模型跨导出分段时，动作、表情和自动眨眼按剧情时间恢复，减少切点处的姿态或表情跳变。
- 同一动作组包含多条动作时，各进程使用一致的动作选择。显式动作的队列起点现在对齐剧本事件时刻，避免异步启动后与恢复段相差数帧；已结束的非循环动作也不会从头重播。
- 多行普通对话改用同一张原位文字截图参与逐字合成，避免第二行个别字符轻微上下跳动。
- 安装器在强制继续后仍无法完成修复时，会提示检查 WebGAL 4.6.4 与 MyGO 3.2.1。
- 修复安装生成式 AI 组件时，深层类型声明文件路径过长可能导致安装中断的问题；运行所需文件仍会正常复制和校验。

## 验证范围

已对照单进程与多进程导出，验证多动作切段，以及 Cubism 4 模型的待机、眨眼和非循环动作场景。模型的物理效果、特殊配置和其他运行环境未全部覆盖；如仍遇到切段跳变，请保留剧本与导出日志以便定位。

## 版本与安装

- 产品版本：`1.0.1`
- 安装器 Win32 版本：`1.0.1.0`
- 导出内核：`0.6.42`
- 适配基线：WebGAL Terre `4.6.4`

正式安装器文件名为 `WebVideo+-Setup-1.0.1.exe`；发布后可从 [GitHub Releases](https://github.com/Stakataka-3030/WebVideo-Plus/releases) 获取。完整的逐版本变更见 [CHANGELOG.md](CHANGELOG.md)。
