# WebVideo+ 1.0.4 发行说明

安装器现在支持在**高级设置**中手动指定已有的 FFmpeg 文件夹。选择含 `ffmpeg.exe` 和 `ffprobe.exe` 的文件夹，或选择其上一级目录（内有 `bin`），安装器验证后即可使用本地文件，无须重新下载。留空时仍按原方式自动检测和下载。

两种方式可以互相切换：自动下载失败时，安装器会提示改选本地 FFmpeg；手动目录无效时，也会提示改用自动检测或下载。下载进度缓存会保留，以便重试。

手动选择的目录会保存在当前 Terre 实例的 WebVideo+ 配置中，供后续更新和导出使用。安装器不会修改系统 PATH，也不会删除该目录；请在使用期间保留其中的 FFmpeg 文件。若系统仍缺少 WebView2 Runtime，其安装提示不受此选项影响。

- 产品版本：`1.0.4`
- 安装器 Win32 版本：`1.0.4.0`
- 内部版本：`0.8.3`
- 导出内核：`0.6.43`（本次未改动）

正式安装器文件名为 `WebVideo+-Setup-1.0.4.exe`。此前版本的改动见 [CHANGELOG.md](CHANGELOG.md)。
