# WebVideo+ 1.0.2 发行说明

本次更新修复了部分 Windows 安装路径下，勾选生成式 AI 组件后安装中断的问题。

安装时，WebVideo+ 需要将 AI 运行组件复制到 Terre 的临时目录。部分依赖包中的 TypeScript 类型声明路径过深，目标路径达到 Windows 传统路径长度边界时，复制会失败并导致安装退出。现在复制过程会跳过 `dist-types`、`.d.ts` 和 source map 等不参与运行的文件；`worker.mjs`、运行用 JavaScript 等必要文件仍须成功复制，失败时不会被掩盖。未选择生成式 AI 组件的安装流程不受此变更影响。

已在本机用 260 字符的目标路径验证跳过行为，同时确认运行文件能复制、运行文件出错仍会中止，并完成正式安装器构建。尚未在最初报错的用户机器上实测。

- 产品版本：`1.0.2`
- 安装器 Win32 版本：`1.0.2.0`
- 导出内核：`0.6.42`（本次未改动）
- 适配基线：WebGAL Terre `4.6.4`

正式安装器为 `WebVideo+-Setup-1.0.2.exe`。此前的 Live2D 切段与文字显示改进见 [1.0.1 发行说明](RELEASE_NOTES_1.0.1.md)；完整记录见 [CHANGELOG.md](CHANGELOG.md)。
